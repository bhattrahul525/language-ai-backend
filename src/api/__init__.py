"""API package."""


class ApiController:
    """Base API controller template for route-level handlers."""

    def register(self) -> None:
        """Override to attach routes, dependencies, and middleware."""
        return None
